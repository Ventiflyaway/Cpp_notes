### 解题思路：树\递归

递归：小问题和原问题是同类

```text
返回值 dfs(TreeNode* root){

    // 1. 结束条件
    if(root == nullptr){
        return ?
    }

    // 2. 子问题
    auto left = dfs(root->left);
    auto right = dfs(root->right);

    // 3. 组合答案
    return ?
}
```