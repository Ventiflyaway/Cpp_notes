### 连续、最长最短、保留某种状态：滑动窗口

#### 最长无重复子序列
- 是否见过：seen，所以用哈希集合
  - erase(), insert()
- 连续：用滑动窗口
- 合法就继续扩、不合法才缩；缩完以后记录最大值
```text
int left = 0;

for(int right = 0; right < nums.size(); right++) {

    // 1. 右指针进入窗口
    // update(nums[right]);

    // 2. 判断是否需要收缩窗口
    while(窗口不满足条件) {

        // left离开窗口
        // remove(nums[left]);

        left++;
    }

    // 3. 更新答案
    // update_answer(...)
}
```

#### 最小子数组
- 合法马上缩：最小值必须在循环内记录！否则丢了