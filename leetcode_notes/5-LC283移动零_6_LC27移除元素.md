给定一个数组 nums，编写一个函给定一个数组 nums，将所有 0 移动到数组的末尾，同时保持非零元素的相对顺序。
在不复制数组的情况下原地对数组进行操作

### 解法：双指针
非0应该放哪里？
- slow：下一个应该放非零元素的位置

### 变量初始化要分开初始化！！
int fast = 0, slow = 0;

### for循环标准格式必须有3段（2个分号！）
for(初始化; 条件; 更新)