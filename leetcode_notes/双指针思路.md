### 双指针
#### 1. 信号一：“原地修改数组”
fast 找元素，slow 放结果，快慢指针压缩数组
- LC27 删除元素、LC283 移动零、LC26 删除重复元素
```text
int slow = 0;

for(int fast = 0; fast < nums.size(); fast++){
    if(满足保留条件){
        nums[slow] = nums[fast];
        slow++;
    }
}
```
#### 2. 信号二：有序数组 + 查找两边关系
左右指针
- LC125 验证回文串、LC167 两数之和 II（有序数组）、LC11 盛最多水的容器
```text
int left = 0;
int right = n - 1;

while(left < right){
    ...
}
```

#### 3. 信号三：连续区间 / 子数组
滑动窗口
- LC3 最长无重复子串

右扩 → 判断 → 左缩 → 更新答案
```text
int left = 0;

for(int right = 0; right < nums.size(); right++) {

    // 1. 右指针进入窗口
    // update(nums[right]);

    // 2. 判断是否需要收缩窗口
    while(窗口不满足条件) {

        // left离开窗口
        // remove(nums[left]);

        left++;
    }

    // 3. 更新答案
    // update_answer(...)
}
```

#### 4. 信号四：链表找环/中点